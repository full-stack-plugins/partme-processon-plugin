# processon-review：正常路径示例

## 请求

用户给出明确目标、输入、交付格式与验收条件，希望使用 `processon-review` 完成一次有界任务。

## 执行

1. 识别触发条件与范围。
2. 冻结审查对象、验收标准、证据时间和版本标识。
3. 展示计划和需要的授权点。
4. 逐项判定 PASS、FAIL、SKIPPED 或 BLOCKED，并记录依据。
5. 输出发现、严重度、证据位置、修复建议和剩余风险。

## 期望输出

返回 `COMPLETED`、真实证据与产物位置；任何未执行检查单独列在 `skipped` 中。
